<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lunch Time</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://code.iconify.design/iconify-icon/1.0.7/iconify-icon.min.js"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
    <link rel="stylesheet" href="/css/style.css">
</head>

<body>
    {{>header}}

    <main>
        <div class="content_admin">
            <ul class="nav nav-tabs">
                <li class="nav-item">
                    <a class="nav-link active" data-bs-toggle="tab" href="#admin">See Feedbacks</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" data-bs-toggle="tab" href="#addProduct">Add Product</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/editProduct">Edit Products</a>
                </li>
            </ul>
            <div class="tab-content">
                <div class="tab-pane container active" id="admin">
                    <div class="feeds">
                        {{#each feedbacks}}
                        <div class="fd_back">
                            <div class="sim">
                                <p>&#10078;</p>
                            </div>
                            <div class="fd_text">
                                <p>{{message}}</p>
                            </div>
                            <div class="fd_name">
                                <p>{{name}} {{surname}}</p>
                                <p>{{email}}</p>
                            </div>
                            <div class="fd_delete">
                                <a href='/delete_feedback/{{id}}'>
                                    <i class="fa fa-trash"></i>
                                </a>
                                {{!-- <br>
                                <input type="radio" checkbox.value = checkbox.checked ? 1 : 0; value="1"><label> 0</label>
                                <input type="radio" {{#if type}}checked{{/if}} value="0"><label> 1</label> --}}
                            </div>
                        </div>
                        {{/each}}
                    </div>
                </div>
                <div class="tab-pane container fade" id="addProduct">
                    <form action="/addProduct" method="post">
                        <div class="mb-2 mt-2">
                            <select name="type">
                                <option value="Dishes">Dishes</option>
                                <option value="Salads">Salads</option>
                                <option value="Drinks">Drinks</option>
                            </select>
                            <br>
                            <input type="text" placeholder="Image Name" name="image"> <br>
                            <input type="text" placeholder="Product Title" name="title"> <br>
                            <input type="number" placeholder="Product Price" name="price"> <br>
                            <button>Add Product</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>

    </main>
<div class="second_footer">

    {{>footer}}
</div>

</body>

</html>